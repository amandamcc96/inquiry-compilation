# Backend

- Fill in `.env` with your real GOOGLE_CLIENT_EMAIL and GOOGLE_PRIVATE_KEY.
- Replace `YOUR_SPREADSHEET_ID_HERE` in `index.js` with your Google Sheet ID.
- Run:

  npm install
  npm start

The backend will start on http://localhost:4000
